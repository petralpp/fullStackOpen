{
  "name": "blogilista",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "node --watch index.js",
    "test": "node --test",
    "build:ui": "rm -rf dist && cd ../frontend/blogiApp && npm run build && cp -r dist ../../backend"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "lodash": "^4.17.21",
    "mongoose": "^8.16.0",
    "morgan": "^1.10.0"
  }
}
